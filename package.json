{
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "setup-hooks": "git config core.hooksPath .githooks && chmod +x .githooks/pre-push && echo 'Git hooks configured'",
    "bump-sw": "node -e \"const fs=require('fs');const v='honeyos-'+new Date().toISOString().replace(/[^0-9]/g,'').slice(0,14);fs.writeFileSync('sw.js',fs.readFileSync('sw.js','utf8').replace(/const CACHE = '[^']*'/,\\\"const CACHE = '\\\"+v+\\\"'\\\"));console.log('SW version:',v);\""
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "utils.js",
      "supabase_client.js"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
