# HoneyOS 設計書

> 最終更新: 2026-09-08  
> ブランチ: `claude/keen-planck-wa39zm`

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [技術スタック](#2-技術スタック)
3. [画面フロー](#3-画面フロー)
3.5. [画面仕様（詳細）](#35-画面仕様詳細)
4. [DB / テーブル設計](#4-db--テーブル設計)
5. [セキュリティ方針](#5-セキュリティ方針)
6. [実装ロードマップ](#6-実装ロードマップ)

---

## 1. プロジェクト概要

**HoneyOS** は養蜂家向けのモバイル PWA（Progressive Web App）。  
蜂群の内検記録・作業管理・分析を一元化し、養蜂の日常業務を効率化することを目的とする。

| 項目 | 内容 |
|------|------|
| 対象ユーザー | 個人・小規模養蜂家 |
| 提供形態 | PWA（iOS Safari / Android Chrome 対応） |
| ホスティング | GitHub Pages（`/HoneyOS/`） |
| バックエンド | Supabase（PostgreSQL + Auth + Realtime） |
| 言語 | 日本語（英語切替あり） |

---

## 2. 技術スタック

### フロントエンド
- **単一ファイル構成**: `index.html`（DCLogic/x-sc マイクロフレームワーク内蔵）
- **テンプレート**: `{{ expr }}` バインディング、`sc-if` / `sc-for` ディレクティブ
- **テーマシステム**: `THEMES` オブジェクト → `t.*` トークンで全スタイルを制御
- **デフォルトテーマ**: `clean`（白ベース + アンバーアクセント）
- **PWA**: `sw.js`（Service Worker）、`manifest.json`

### バックエンド
- **Supabase**: 認証・DB・Realtime・Push通知
- **DB アクセス層**: `supabase_client.js` → `window.HoneyDB` として公開

### 補助ファイル
| ファイル | 役割 |
|---------|------|
| `supabase_client.js` | DB操作のラッパー（全 CRUD + Realtime + Push） |
| `supabase_schema.sql` | テーブル定義・RLSポリシー |
| `support.js` | ユーティリティ（日付・計算系） |
| `utils.js` | 汎用ヘルパー |
| `sw.js` | Service Worker（Cache-first / Network-first 振り分け） |
| `component.js` | 追加コンポーネント（将来拡張用） |

---

## 3. 画面フロー

### 認証フロー（ログイン前）

```
[起動]
  │
  ├─ 未ログイン → SCR-001 ログイン
  │                ├─ 新規登録 → SCR-002 新規登録
  │                │               └─ SCR-003 オンボーディング① ようこそ
  │                │                   └─ SCR-004 オンボーディング② 養蜂場設定
  │                │                       └─ SCR-005 オンボーディング③ 準備完了
  │                │                           └─ [メイン画面へ]
  │                └─ ログイン成功 → [メイン画面へ]
  │
  └─ ログイン済み → [メイン画面へ]
```

### メイン画面（ボトムナビ 5タブ）

```
┌──────────────────────────────────────┐
│  ホーム │ 養蜂場 │ 作業 │ 分析 │ 設定 │
└──────────────────────────────────────┘
```

#### ホームタブ
```
SCR-006 ダッシュボード
  ├─ 通知ベル → SCR-007 通知センター
  └─ 内検を始める → SCR-011 内検開始
```

#### 養蜂場タブ
```
SCR-008 蜂群一覧（旧: 蜂群サマリー）
  ├─ ヘッダー: リスト⇔マップ切替アイコン → SCR-010 養蜂場マップ
  │   ※ v1.0はアイコン切替（案1）。将来的にセグメントコントロールでの
  │      常設タブ内タブ化（案3）へ拡張する可能性あり（マップ機能拡充時に再検討）
  └─ カードタップ → SCR-009 蜂群（巣箱）詳細（旧: 養蜂場詳細を廃止し番号再利用。
                     旧SCR-009b 巣箱サマリーの内容を統合。1蜂群=1巣箱前提）
                     ├─ 内訳推移グラフの点タップ → 内検詳細ポップアップ（簡易版）
                     │   └─ 「枠ビューアで見る」→ SCR-013 枠ビューア（スワイプで過去の内検日に遡れる）
                     ├─ 「内検履歴を見る」導線 → SCR-013 枠ビューアへ直接遷移（履歴一覧画面は挟まない）
                     ├─ センサーデータ要約 → SCR-017 センサー詳細
                     │                        └─ SCR-018 センサーグラフ（種別パラメータで切替、旧018-020を統合）
                     └─ カメラ画像 → SCR-021 カメラ画像

※ 旧SCR-009「養蜂場詳細」は廃止（機能はSCR-008に統合）
※ SCR-016「内検履歴ダッシュボード」・SCR-016b「内検詳細」は廃止
   （内容はSCR-009のポップアップ＋SCR-013に統合）
```

#### 作業タブ
```
SCR-024 作業（カレンダー + タスク一覧、タブ切替）
  ├─ カレンダータブ: 日付タップ → SCR-025 タスク作成（日付プリセット）
  ├─ リストタブ: チェックボックスで即完了
  ├─ SCR-025 タスク作成
  └─ SCR-026 作業記録入力（種別ごとの動的フォーム）
      └─ タスク完了時「作業記録を残す」→ 種別・蜂群を自動引き継ぎ
  SCR-027 作業履歴（フラットリスト＋種別フィルタ）⇄ SCR-028 レポート（双方向導線）
```

#### 分析タブ
```
SCR-028 レポート（月次/年次、養蜂場別・蜂群別切替、PDF/CSV導線はv2.0実装）
SCR-029 蜂群トレンド（個別群比較 / 全体平均のタブ切替）
SCR-030 蜂群比較（表形式 ⇄ 横棒ランキング）
```

#### 設定タブ
```
SCR-031 設定
  ├─ プロフィールカード（ユーザー名・養蜂場名） → アカウント編集
  ├─ アカウント: プロフィール編集 / パスワード変更 / アカウント削除 / ログアウト
  ├─ アプリ設定: 通知設定 / Push通知許可状態確認 / テーマ切替 / 言語切替
  ├─ 養蜂固有設定: 「蜂群の強さ」算出式カスタマイズ / デフォルト記録方式（枠式/割合式）
  ├─ データ管理: 養蜂場・蜂群の管理（SCR-032 蜂群新規追加 / SCR-033 養蜂場新規追加）/
  │              データエクスポート（v1.0: JSON、v2.0: CSV/PDF）/ 同期状況（未送信データ確認）
  └─ アプリ情報: お問い合わせ・フィードバック / 利用規約・プライバシーポリシー / バージョン情報

※ SCR-001「パスワードを忘れた方」→ SCR-034 パスワードリセット
※ 蜂群・養蜂場の削除は確認ダイアログ＋ゴミ箱/アーカイブ（無期限保持、設定タブから完全削除）
```

#### 内検フロー（ホームまたは養蜂場から起動）
```
SCR-011 内検開始（蜂群・日付選択、SCR-008相当のカード一覧を流用）
  └─ SCR-012 内検記録（段→枠 2階層、記録方式は枠式/割合式を選択。
      ベースはSCR-013と同じ見た目＋入力特化コントロールのハイブリッド）
      ├─ SCR-014 AI解析入力（内検フロー内でも、SCR-021等から単独でも起動可）
      │   └─ SCR-022 AI診断結果
      │       └─ SCR-023 AI推奨作業 → SCR-025 タスク作成 / SCR-026 作業記録入力
      └─ SCR-015 内検サマリー（完了）
          ├─ 編集導線 → SCR-012へ戻る
          ├─ リマインダー設定（次回内検予定日）
          └─ 次のアクション: ダッシュボードへ戻る / 次の蜂群の内検を続ける / 作業記録を追加
```

---

## 3.5 画面仕様（詳細）

> 各画面に何を表示するかを一問一答形式で詰めた合意事項。未記載の画面は今後詰める。

### SCR-001 ログイン
- メール/パスワード入力欄
- 「パスワードを忘れた方」リンク
- 新規登録への導線
- 「ログイン状態を保持」チェックボックス
- エラー表示: 一般的な形（認証失敗・未確認メール等）

### SCR-002 新規登録
- 入力項目: メール、パスワード、名前（必須）、養蜂場名（任意）
- パスワード強度バリデーション表示: 今は無し
- 利用規約・プライバシーポリシー同意チェック: 今は無し（将来的に必要）

### SCR-003 オンボーディング① ようこそ
- スライド形式でアプリの機能を簡単に紹介

### SCR-004 オンボーディング② 養蜂場設定
- 設定項目: 養蜂場名、所在地、蜂群の初期登録、巣箱数
- すべて任意（スキップ可）

### SCR-005 オンボーディング③ 準備完了
- 「最初の内検を始める」ボタン
- 「あとで」スキップ導線も用意

### SCR-006 ダッシュボード（ホームタブ）
デザイン方針: 数字・グラフによる視覚的わかりやすさを重視。

構成（上から順）:
1. **警告バナー**（要注意群がある場合のみ表示）
   - 例:「⚠️ 2群が要注意」の数字バッジ表示
   - タップで該当蜂群へジャンプ
2. **通知ベルアイコン**（ヘッダー右上、控えめ）→ SCR-007へ
3. **天気・センサー情報**（コンパクト）
   - 気温: 過去24時間推移のミニスパークライン
   - 湿度・風速: アイコン+数値で1行横並び
4. **蜂群サマリー**（中段・メインコンテンツ）
   - 指標名「蜂群の強さ」（0〜100）
   - バー上部: 現在値
   - 赤い破線: 注意ライン60
   - バー下部: 前回内検からの増減
   - 「算出方法」導線: 内訳を表示（蜂量40%+育児量30%+貯蜜量20%+女王状態10%）
   - 備考: 季節補正は将来対応。算出式は暫定運用
5. **今週の統計**（下段）
   - 採蜜量・作業件数の数値
   - 先週比の増減矢印（例: ↑12%）
   - 簡易スパークライン（直近4週間の採蜜量推移）
6. **空状態**（蜂群未登録時）: オンボーディング直後のガイダンス表示
7. **フローティングボタン**「内検を始める」（画面右下固定、全セクションに重なる）

### SCR-007 通知センター
- 通知種別: 内検リマインダー／AI診断完了／センサー異常アラート／システムお知らせ
- 既読/未読管理あり
- タップで種別ごとの紐づく画面へ遷移

### SCR-008 蜂群一覧（旧: 蜂群サマリー、養蜂場タブ）
ダッシュボードの「蜂群サマリー」とは役割が異なるため名称変更。全養蜂場・全蜂群を横断的に一覧・比較する画面。

**画面上部**
- 養蜂場セレクタ（タブ切替: 「全て」+各養蜂場名）
- 検索バー（名前検索）
- フィルタ（必須機能）: 状態別（良好/注意/危険）、巣箱タイプ別、最終内検日など
- ソート: デフォルトは**最終内検日順**。他に強さスコア順・名前順など選択可。選択したソート順は次回訪問時も記憶（永続化）
- 新規蜂群追加ボタン
- ヘッダー: リスト⇔マップ切替アイコン → SCR-010 養蜂場マップ
  - v1.0はアイコン切替。将来的にセグメントコントロールでの常設タブ内タブ化へ拡張する可能性あり（マップ機能拡充時に再検討）

**カードグリッド**（養蜂場ごとにセクション見出し、無限スクロール）
各カードの表示要素:
- 蜂群名
- 積み上げバー: **全枠平均の構成比（%）**で「蜂・育児・貯蜜・空間」を色分け表示
  - スコアではなく実測値ベース（`insp_records.frame_details`の集計）
  - 枠式・割合式は同一ロジックで統一表示
- 前回比: 内訳の変化（例: 育児量↑）
- 最終内検日（相対表示、例:「3日前」）
- 状態アイコン（信号色: 良好/注意/危険）

**カードタップ** → SCR-009 蜂群（巣箱）詳細へ遷移

### SCR-009 蜂群（巣箱）詳細（旧: 養蜂場詳細を廃止し番号再利用。旧SCR-009b「巣箱サマリー」を統合）
前提: **1蜂群=1巣箱**としてシンプルな構造。

優先度順の構成:
1. **内訳推移グラフ**（蜂・育児・貯蜜、最上位・メイン）
   - グラフ上の点（日付）をタップ → その日の内検詳細をポップアップ表示
     - ポップアップ内容（スリム化版。総蜂数・女王確認・枠内訳はSCR-013側で確認するため重複させない）:
       - 内検日・時刻・天気
       - メモ（内検メモ・AI診断メモ）
       - 「枠ビューアで見る」ボタン → SCR-013へ本格遷移
   - 別途「内検履歴を見る」導線 → SCR-013へ直接遷移（履歴一覧画面は挟まず、SCR-013側のスワイプで過去日を遡る）
2. **強さスコア推移グラフ**（長期間）
3. **センサーデータ**要約（SCR-017/018と連携）
4. **作業記録履歴**（採蜜・給餌・治療など、この群に関するもの）
5. **カメラ画像**（SCR-021と連携）
6. **AI診断結果の履歴**
7. **蜂群の基本情報編集**（名前・巣箱タイプなど）

### SCR-010 養蜂場マップ
用途: 複数養蜂場を管理する上での地理的把握、移動時の利便性、蜜源植物ピンなどの活用。
- アクセス動線: SCR-008ヘッダーのリスト⇔マップ切替アイコンから（v1.0）
- 将来: マップ機能拡充時に、養蜂場タブ内でのセグメントコントロール常設化（一覧と対等な主要ビュー化）を再検討

### SCR-011 内検開始（蜂群・日付選択）
**蜂群選択**（SCR-008相当のカード一覧を流用。内検開始の文脈でも検索・フィルタ・ソートは必要）
- 養蜂場セレクタ（タブ）
- 検索バー
- フィルタ（状態別/巣箱タイプ別/最終内検日など）
- ソート（デフォルト: 最終内検日順）
- カードグリッド: 蜂群名、積み上げバー（前回内検の構成比）、前回比、最終内検日、状態アイコン
  - 前回までの状態を見てから内検に入れることで、「何を注意して見るべきか」の判断材料になる
- カードタップ → 選択して次へ（日付選択へ）

**日付選択**
- デフォルト: 今日
- 過去日付も選択可能（後から記録する場合）

**天気**
- 自動取得（位置情報＋無償気象API、OpenMeteo等を想定）

### SCR-012 内検記録（段→枠 2階層）
**記録方式の選択（枠式／割合式）**
- SCR-011の後、記録に入る前にトグルで選択
- デフォルトはその蜂群の前回記録方式を自動選択（毎回選び直す手間を削減）
- 同じ蜂群でも回によって枠式／割合式を切り替えて記録可能

**入力UI（ハイブリッド: SCR-013の見た目 + 入力特化コントロール）**
- ベースはSCR-013（枠ビューア）と同じ見た目（段を縦に並べたスクロール表示、配色・凡例共通）
- 枠バーをタップ → 入力特化のコントロール（スライダー／+-ボタン等）がその場でポップアップ／展開し、蜂・育児・貯蜜の割合、女王確認、巣礎/空巣房の切替を調整
- 入力中はリアルタイムでバー表示に反映
- 段の追加・削除はSCR-013と同じ操作（+ボタン／⋮メニュー）を流用

**枠式／割合式によるデータの違い**
- 枠式: 1枠=1本のバー（枠番号ごとに個別入力）。保存先は `frame_details`
- 割合式: `space_count` 本のバーに分解して表示・入力（枠単位ではなく大まかな割合として記録）。保存先は `space_levels`
- どちらの方式でも入力後の表示ロジック（積み上げバー・配色・段構成など）は共通UIを使用し、内部の保存先のみ方式によって変わる

### SCR-013 枠ビューア
参考: Beewiseのダッシュボード（1枠=1本の縦バー、内訳を色分けセグメントで積み上げる表現）。

**基本表示**
- 段（継箱）を実物どおり縦に並べてスクロール表示。隔王板は段の境界線として表現
- 枠式（`frame_details`）: 1枠=1本のバー、蜂/育児/貯蜜の割合で色分けセグメント
- 割合式（`space_levels`）: `space_count`本のバーに分解し、同様に表示（枠式と統一ロジック。精密に見えても許容）
- 空き部分は「巣礎（Foundation）」と「空巣房（Empty Comb）」を区別して表示
- 給餌器は枠バーの一種として表示しない
- 各バーの下に枠番号を表示（1, 2, 3...）
- 画面上部: 凡例（色の意味）を常時表示 + 総蜂数（推定）を常時表示

**配色**: HoneyOSのテーマカラー（白＋アンバー）を基調にブランド一貫性を優先
- 貯蜜: アンバー/ゴールド
- 育児: ローズ/コーラル系
- 蜂: チャコール系
- 空き: 薄いグレー
- 女王ステータス: 信号色（産卵中=緑、未確認=グレー、不安=赤）

**女王表示**: 王冠アイコン＋ステータス色分け（産卵中/未確認/不安等）

**インタラクション**
- 枠バーをタップ → 詳細ポップアップ
- この画面から直接データ編集が可能
- 過去の内検日への切り替え: スワイプで巣箱全体（全段）を一括切り替え
- 段の追加: 巣箱ビュー最上部・最下部に「+ 段を追加」ボタン
- 段の削除: 各段見出しの「⋮」メニュー →「この段を削除」→ 確認ダイアログ

**将来拡張**
- AI診断（SCR-014/022/023）と連携し、枠バーに異常マーク（⚠️）を重ねる拡張余地を確保

### SCR-016 内検履歴ダッシュボード / SCR-016b 内検詳細
**廃止。** SCR-009の内訳推移グラフ（点タップ→ポップアップ→SCR-013）で内検履歴を遡る動線が既にカバーされているため、独立した一覧・詳細画面は設けない。

### SCR-017 センサー詳細
- 対象センサー種別: 温度・湿度・重量・音響など（v1.5ロードマップ「センサーデータの外部連携」に対応）
- 構成: センサー種別ごとのカード一覧（各カード: 現在値＋簡易推移グラフ）
- カードタップ → SCR-018（統合版センサーグラフ）へ遷移
- センサー未設置の種別: グレーアウト表示＋「センサー未設置」案内。タップで設置方法の案内／センサー連携設定画面へ誘導

### SCR-018 センサーグラフ（旧SCR-018〜020を統合）
- 個別のセンサー種別ごとに画面を分けず、1つの画面テンプレートをセンサー種別パラメータで切り替えて使用
- 期間選択: 日／週／月のプリセット＋カスタム範囲指定
- グラフ形式: 折れ線グラフ

### SCR-021 カメラ画像
- 蜂群に設置されたカメラ（inspection robot含む）の画像を確認する画面
- 撮影方法: ロボットによる自動定期撮影＋養蜂家による手動アップロード、両方に対応
- 表示形式: 静止画ギャラリー（撮影日時順）が基本。ライブ映像は将来拡張として枠を残す
- 内検記録・作業記録と紐づけてグルーピング表示（例:「この内検の時の写真」）
- 各画像から「AI解析にかける」ボタン → SCR-014（AI解析入力）へ

### SCR-014 AI解析入力
- **起動導線**: SCR-012内検記録完了後のフローの一部として遷移可能。かつSCR-021等から単独でも起動可能（両対応）
- **入力データ**:
  - カメラ画像（任意。アップロード／撮影）
  - 内検記録データ（枠内訳・女王確認・総蜂数などを自動連携、必須）
- **AI解析対象**: 蜂群の健康状態診断、病気・害虫の検出、女王の有無判定などを推定
- 画像がなくても内検データのみで解析実行可能

### SCR-015 内検サマリー（完了）
**表示内容**
- 今回の入力内容の要約
- AI診断結果のハイライト（あれば）
- 前回との比較
- 警告の強調表示: 注意ライン（60）を下回った、AI診断で異常検出等があれば、SCR-006と同じビジュアル言語で警告バナー表示

**アクション導線**
- ダッシュボードに戻る
- 次の蜂群の内検を続ける
- 作業記録を追加する
- 編集導線: SCR-012（内検記録）へ戻って修正
- リマインダー設定: 次回内検予定日の確認・設定（v1.5 内検リマインダー機能と連携）

### SCR-022 AI診断結果
- 信頼度スコア表示
- 画像上に検出箇所をハイライト表示
- テキストでの説明

### SCR-023 AI推奨作業
- 診断結果を受けた推奨アクション（給餌・治療など）を提示
- SCR-025（タスク作成）またはSCR-026（作業記録入力）へ直接つなげる導線

### SCR-024 作業（カレンダー＋タスク一覧）
**表示形式**: タブ切替（カレンダー／リスト）

**カレンダータブ**
- 月表示
- イベント種別を色分け表示: 内検リマインダー、作成したタスク、実施済みの作業記録、季節の目安イベント（ユーザーが自分で登録）
- 日付タップで新規タスク作成（SCR-025）へ、その日付が初期値として入る

**リストタブ**
- 未完了タスクを優先度・期限順でフラット一覧
- チェックボックスタップで即完了

**共通**
- フィルタ: 養蜂場別・蜂群別
- タスク属性: 優先度（高/中/低）、蜂群との紐づけ（SCR-009側からも該当タスクが見える）

### SCR-025 タスク作成
**入力項目**
- タイトル（必須）
- 期限日（カレンダーから遷移時はプリセット、それ以外は任意選択）
- 優先度（高/中/低）
- 紐づける蜂群（複数蜂群にまたがるタスクも想定。例:「全群に給餌」）
- 紐づける養蜂場（蜂群選択で自動決定）
- メモ（自由記述）

**編集・削除**: 作成後も編集・削除とも可能

**リマインダー通知**: 期限前に通知（SCR-007通知センターと連携）

**繰り返し設定**
- v1.0: 繰り返しなし、単発タスクのみ（シンプルに開始）
- v1.5以降: 「毎週」「毎月」「N日ごと」等の繰り返しルールをロードマップに追加検討

### SCR-026 作業記録入力
**入力フォーム**: 種別ごとの動的フォーム切替

- 共通項目: 種別、蜂群、日付、時刻、メモ
- 採蜜: 採蜜量（kg）、採蜜方法（遠心分離/圧搾など）、単群/複数群まとめての区別
- 給餌: 給餌の種類（砂糖水/花粉代替品など）、給餌量
- 治療: 使用薬剤名、処置内容、次回投薬予定日
- 分蜂: 分蜂の種類（自然/人工）、新群登録へのショートカット
- 越冬: 越冬準備の内容（保温材設置、給餌量など）
- 写真添付（任意、SCR-021カメラ画像と連携）

**編集・削除**: 記録は作成後も編集・削除とも可能

**タスク連携**
- SCR-025のタスク完了時、「作業記録を残す」オプションから、種別・蜂群がタスク内容から自動引き継がれた状態でSCR-026を起動

### SCR-027 作業履歴
**表示形式**: ハイブリッド
- デフォルトは時系列フラットリスト
- 画面上部に種別フィルタチップ（採蜜/給餌/治療/分蜂/越冬）で絞り込み可能
- リスト項目タップ → SCR-026を編集モードで開く（詳細確認・修正が可能）

**フィルタ**: 養蜂場別・蜂群別・種別別・期間別

**分析タブとの連携**
- SCR-027 →「詳しく分析する」リンク → SCR-028レポートへ
- SCR-028側にも「履歴を見る」導線 → SCR-027へ（双方向）

### SCR-028 レポート
**基本方針**: 月次/年次の自動生成サマリーレポート、既定フォーマット＋項目カスタマイズ追加可能

**既定フォーマットの項目**: 採蜜量、作業件数、内検実施率、強さスコア推移、AI異常検出件数

**対象範囲**: 養蜂場別・蜂群別に切り替え可能

**エクスポート**: PDF/CSVエクスポート導線をこの画面に配置（実装はv2.0ロードマップで対応）

### SCR-029 蜂群トレンド
SCR-009（蜂群詳細）が「現場での記録確認」なのに対し、SCR-029は「分析・比較・長期的な経営視点」の画面という役割分担。

**ビュー切替（タブ）**
- 個別群比較: 複数蜂群をチェックボックスで自由選択し、折れ線を重ねて表示（色分け）。指標の一つとしてレーダーチャート（強さスコアの内訳: 蜂量・育児量・貯蜜量・女王状態）も選択可能
- 全体平均: 養蜂場or全体の平均線＋バラつき（最大/最小の範囲）表示

**表示指標**: 強さスコア、内訳（蜂・育児・貯蜜）、採蜜量、内検頻度

**期間指定**: プリセット（3ヶ月/1年/全期間）＋カスタム範囲指定

**年次比較**: 「今年 vs 去年の同時期」を同一グラフ上に2本の線として重ねて表示

**異常のハイライト**
- v1.0: グラフを見て気づく形（自動強調なし）
- 将来: 自動で異常な動きの群を強調表示する機能を追加検討

### SCR-030 蜂群比較
SCR-029（トレンド＝時間軸での変化）に対し、SCR-030は「ある一時点での複数蜂群を横並びで比較する」画面。

**表示形式**: ハイブリッド
- デフォルト: 表形式（縦=蜂群、横=指標のマトリクス表。強さスコア・蜂量・育児量・貯蜜量・最終内検日・採蜜量累計など。ソート可能）
- 指標フォーカス時: 横棒グラフ（ランキング形式）に切替可能
- レーダーチャートはこの画面ではなく、SCR-029「個別群比較」ビュー側に指標として組み込む

### SCR-031 設定（新規定義）
**画面上部**
- プロフィールカード（ユーザー名・養蜂場名を表示、タップでアカウント編集へ）

**アカウント**
- プロフィール編集、パスワード変更
- アカウント削除（退会）
- ログアウト

**アプリ設定**
- 通知設定（種別ごとに個別ON/OFF: 内検リマインダー／AI診断完了／センサー異常アラート／システムお知らせ）
- Push通知の許可状態確認（ブラウザ側通知がオフの場合は案内表示）
- テーマ切替（clean/honey等）
- 言語切替（日本語/英語。今回追加した要素（積み上げバーの凡例、AI診断結果の文言など）も多言語対応の対象に含む）

**養蜂固有設定**
- 「蜂群の強さ」算出式のカスタマイズ（将来、季節補正等が実装された際の養蜂家ごとの微調整用）
- デフォルトの記録方式（枠式/割合式）の初期設定

**データ管理**
- 養蜂場・蜂群の管理（一覧・追加・編集・削除）
- データエクスポート（v1.0: JSON、v2.0: CSV/PDF）

**アプリ情報**
- お問い合わせ・フィードバック送信
- 利用規約・プライバシーポリシー
- アプリバージョン情報

---

### SCR-032 蜂群新規追加
- 入力項目: 蜂群名（必須）、所属養蜂場（必須。0件の場合は先に養蜂場登録を促す）
- 巣箱タイプ・段構成は登録時には設定せず、初回内検の記録方式選択（枠式/割合式）と連動して自動決定
- 登録直後（内検前）のSCR-008での見え方: 積み上げバーは空表示＋「内検未実施」の案内

### SCR-033 養蜂場新規追加
- 入力項目: 養蜂場名、所在地
- 所在地入力: SCR-010養蜂場マップ上でピンを立てる形式

### SCR-034 パスワードリセット
- SCR-001「パスワードを忘れた方」から遷移
- 一般的なフロー: メールアドレス入力 → 送信 →「メールを送信しました」案内 →（メール内リンクから）新パスワード設定画面

### 蜂群の基本情報編集（SCR-009内）
- インライン編集方式（名前・巣箱タイプなどの表示テキストをタップしてその場で直接編集、画面遷移なし）

### 横断的なUIパターン

**削除フロー（蜂群・養蜂場、v1.0ロードマップ1.2に対応）**
- 削除確認ダイアログに「関連する内検記録◯件・作業記録◯件も削除されます」と具体的な件数を表示
- 確認ステップは1回のダイアログのみ（追加の入力確認は不要）
- 削除後は即完全削除ではなく、ゴミ箱/アーカイブで無期限保持。設定タブから手動で完全削除するまで復元可能

**オフライン時の操作キュー（v1.0ロードマップ1.3に対応）**
- 未送信データがある場合のみ、全タブ共通ヘッダーに小さいインジケーター（バッジ数字付き）を表示
- インジケーターのタップ、または設定タブの「同期状況」から未送信内容（内検記録◯件・作業記録◯件など）を一覧確認可能
- 再接続時は自動送信（ユーザー操作不要）

**エラー表示の統一（トースト→エラーバナー、v1.0ロードマップ1.4に対応）**
- 表示位置: 画面上部固定
- 重大度で表示の重さを分ける（軽微: 通信の瞬断など／致命的: 保存失敗など）
- 自動では消えず、ユーザーが手動で閉じるまで表示し続ける

**空状態（空リスト）の表示統一**
- SCR-006の空状態（蜂群未登録時のガイダンス表示）と同じ考え方を全画面に適用: 「なぜ空なのか」「次に何をすべきか」を示すガイダンス＋導線をセットで表示する
- SCR-024（タスクなし）: 「タスクはありません」＋「＋タスクを作成」導線
- SCR-027（作業履歴なし）: 「作業記録がまだありません」＋「作業記録を追加」導線
- SCR-029／SCR-030（データ不足で描画不可）: 「内検データが2回分たまるとトレンドが表示されます」等、表示できない理由の説明＋内検を促す導線

### 未定（今回のスコープ外・意図的に保留）
- **ロボット連携の技術詳細**: SCR-021のロボット自動撮影について、ロボットとの通信・同期方式（プロトコル、アップロードタイミング、認証方式等）は未検討。実装検討時に別途詰める
- **v2.0「チーム共有」機能の画面仕様**: ロードマップ3.3（複数養蜂家のチーム共有）は画面仕様に未着手。v2.0着手時期に改めて詰める
- **匿名ベンチマーク統計への参加可否設定**: SCR-031設定タブの候補として挙がったが、今回は対象外

---

## 4. DB / テーブル設計

### テーブル一覧

| テーブル名 | 説明 |
|-----------|------|
| `auth.users` | Supabase 組み込み認証ユーザー |
| `profiles` | ユーザープロファイル（名前・養蜂場名） |
| `colonies` | 蜂群マスタ |
| `farms` | 養蜂場マスタ |
| `insp_records` | 内検記録 |
| `work_records` | 作業記録（採蜜・給餌・治療 等） |
| `tasks` | タスク（新規） |
| `push_subscriptions` | Web Push 通知購読情報 |
| `benchmarks` | 匿名統計（全ユーザー平均健康度） |

> 2026-09-08 画面仕様の詳細化に伴い、`colonies`/`farms`/`insp_records`/`work_records` にカラムを追加し、`tasks` テーブルを新設（詳細は各テーブル定義を参照）。

---

### `profiles`

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `uuid` PK | `auth.users.id` と 1:1 |
| `name` | `text` | ユーザー表示名 |
| `farm_name` | `text` | 養蜂場名 |
| `created_at` | `timestamptz` | |

> サインアップ時に `handle_new_user()` トリガーで自動作成。

---

### `colonies`

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `text` | 蜂群ID（例: `A-01`）|
| `user_id` | `uuid` | FK: `auth.users` |
| `farm_id` | `bigint` | FK: `farms.id`（所属養蜂場） |
| `name` | `text` | 表示名 |
| `colony_type` | `text` | `'frame'`（枠式）/ `'frameless'`（フレームレス） / `null`（未確定、初回内検時に自動決定） |
| `sort_order` | `integer` | 並び順 |
| `archived_at` | `timestamptz` | ゴミ箱/アーカイブ日時。NULLなら有効、値があれば削除済み（無期限保持、設定タブから完全削除） |
| `created_at` | `timestamptz` | |

> PK は `(id, user_id)` の複合キー。

---

### `farms`

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `bigint` auto PK | |
| `user_id` | `uuid` | FK: `auth.users` |
| `name` | `text` | 養蜂場名 |
| `latitude` | `numeric` | 緯度（SCR-010養蜂場マップ・SCR-033ピン設置用） |
| `longitude` | `numeric` | 経度（同上） |
| `sort_order` | `integer` | 並び順 |
| `archived_at` | `timestamptz` | ゴミ箱/アーカイブ日時。NULLなら有効（`colonies`と同様の削除フロー） |
| `created_at` | `timestamptz` | |

---

### `insp_records`（内検記録）

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `bigint` auto PK | |
| `user_id` | `uuid` | FK: `auth.users` |
| `colony` | `text` | 蜂群ID |
| `date` | `text` | 内検日（`YYYY-MM-DD`） |
| `time` | `text` | 内検時刻 |
| `weather` | `text` | 天気（自動取得） |
| `frames` | `integer[]` | 枠番号リスト |
| `count_mode` | `text` | `'frame'`（枠式）or `'space'`（割合式） |
| `frame_details` | `jsonb` | 枠ごとの詳細データ |
| `space_count` | `integer` | 割合式の総スペース数 |
| `space_levels` | `jsonb` | 割合式の各スペースレベル |
| `structure` | `jsonb` | 段（継箱）構成・隔王板の位置（SCR-013の段別表示に対応） |
| `queen_present` | `boolean` | 女王確認（`queen_status`から導出。後方互換のため残置） |
| `queen_status` | `text` | 女王ステータス: `'laying'`（産卵中）/ `'unconfirmed'`（未確認）/ `'concern'`（不安） |
| `bees_total` | `integer` | 総蜂数概算 |
| `frame_memo` | `text` | メモ |
| `ai_memo` | `text` | AI診断メモ |
| `created_at` | `timestamptz` | |

**`frame_details` / `space_levels` の JSON 構造例:**
```json
{
  "1": { "bees": 80, "brood": 60, "honey": 40 },
  "2": { "bees": 100, "brood": 80, "honey": 20 }
}
```

**`structure` の JSON 構造例:**
```json
{
  "boxes": [
    { "box_no": 1, "frame_range": [1, 8], "has_queen_excluder_above": true },
    { "box_no": 2, "frame_range": [9, 16], "has_queen_excluder_above": false }
  ]
}
```

---

### `work_records`（作業記録）

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `bigint` auto PK | |
| `user_id` | `uuid` | FK: `auth.users` |
| `type` | `text` | 作業種別（採蜜・給餌・治療・分蜂・越冬 等） |
| `colony_ids` | `text[]` | 対象蜂群IDのリスト（複数群にまたがる作業に対応。単一の場合は要素1つ） |
| `date` | `text` | 作業日（`YYYY-MM-DD`） |
| `time` | `text` | 作業時刻 |
| `memo` | `text` | メモ |
| `yield_kg` | `decimal` | 採蜜量 kg（採蜜時のみ） |
| `harvest_method` | `text` | 採蜜方法（例: `'centrifuge'` 遠心分離 / `'press'` 圧搾）（採蜜時のみ） |
| `feed_type` | `text` | 給餌の種類（例: 砂糖水・花粉代替品）（給餌時のみ） |
| `feed_amount` | `text` | 給餌量（給餌時のみ） |
| `medication_name` | `text` | 使用薬剤名（治療時のみ） |
| `next_treatment_date` | `text` | 次回投薬予定日（治療時のみ） |
| `swarm_type` | `text` | 分蜂の種類（`'natural'` 自然 / `'artificial'` 人工）（分蜂時のみ） |
| `photo_urls` | `jsonb` | 添付写真のURLリスト（任意、SCR-021カメラ画像と連携） |
| `created_at` | `timestamptz` | |

---

### `tasks`（タスク、新規）

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `bigint` auto PK | |
| `user_id` | `uuid` | FK: `auth.users` |
| `title` | `text` | タスク名（必須） |
| `due_date` | `text` | 期限日（`YYYY-MM-DD`） |
| `priority` | `text` | 優先度: `'high'` / `'medium'` / `'low'` |
| `colony_ids` | `text[]` | 紐づく蜂群IDのリスト（複数群にまたがるタスクに対応） |
| `farm_id` | `bigint` | FK: `farms.id`（蜂群選択から自動決定） |
| `memo` | `text` | メモ |
| `is_completed` | `boolean` | 完了フラグ |
| `completed_at` | `timestamptz` | 完了日時 |
| `reminder_enabled` | `boolean` | 期限前リマインダー通知の有無 |
| `recurrence_rule` | `jsonb` | 繰り返しルール（v1.5以降で使用。v1.0では常にNULL） |
| `created_at` | `timestamptz` | |

---

### `push_subscriptions`

| カラム | 型 | 説明 |
|-------|-----|------|
| `id` | `bigint` auto PK | |
| `user_id` | `uuid` | FK: `auth.users` |
| `endpoint` | `text` | Push エンドポイント URL |
| `p256dh` | `text` | 暗号化キー |
| `auth_key` | `text` | 認証キー |
| `created_at` | `timestamptz` | |

> `(user_id, endpoint)` にユニーク制約。

---

### `benchmarks`（匿名統計）

| カラム | 型 | 説明 |
|-------|-----|------|
| `user_id` | `uuid` PK | |
| `avg_health` | `numeric` | ユーザーの平均健康スコア |
| `colony_count` | `integer` | 管理蜂群数 |
| `updated_at` | `timestamptz` | |

---

## 5. セキュリティ方針

### 大原則

> **「セキュリティだけは一番気を付けて」**  
> RLS は全テーブルで有効。コンポーネントコードから `window.supabase` を直接叩くことを禁止し、必ず `window.HoneyDB` を経由する。

---

### Row Level Security（RLS）

全テーブルで RLS を有効化。ポリシーは以下の原則に従う:

| 操作 | ポリシー条件 |
|------|------------|
| SELECT | `auth.uid() = user_id` |
| INSERT | `auth.uid() = user_id` |
| UPDATE | `auth.uid() = user_id` |
| DELETE | `auth.uid() = user_id` |

→ 他ユーザーのデータには一切アクセスできない。

---

### DB アクセス層（HoneyDB）

```
コンポーネント (index.html)
       ↓ window.HoneyDB のみ
supabase_client.js
       ↓
Supabase SDK (window.supabase)
       ↓
Supabase API + RLS
```

- `index.html` 内から `window.supabase` を直接呼ぶことを禁止
- 全 DB 操作は `window.HoneyDB.*` 関数を経由する
- 各 HoneyDB 関数はセッション確認を行い、未ログイン時は例外を投げる

---

### 認証

- **Supabase Auth**（メール + パスワード）を使用
- `persistSession: true` / `autoRefreshToken: true` でセッション自動更新
- パスワードリセットは `resetPasswordForEmail()` → メールリンク方式

---

### Service Worker とキャッシュ

- アプリシェル（HTML/JS/画像）は **Cache-first** で配信
- Supabase API / Sentry / OpenMeteo / Nominatim は **Network-first**（キャッシュに古い認証情報が残らない）
- キャッシュ名は `honeyos-YYYYMMDD-X` 形式でデプロイごとにバンプ → 旧キャッシュを自動削除

---

### Push 通知

- `push_subscriptions` テーブルに端末キーを保存（RLS で保護）
- Service Worker の `push` イベントハンドラで受信・表示
- 通知クリックで該当画面へ遷移

---

### データエクスポート

- `HoneyDB.exportAllData()` でユーザー自身のデータのみを JSON エクスポート
- Supabase の RLS により、他ユーザーデータの混入は構造上不可能

---

## 6. 実装ロードマップ

> 2026-09-08: 画面仕様の詳細化に伴い、既存実装（v0.x）から大幅な作り直しが必要な項目が判明したため、ロードマップを再構成。
> 既存コード（`index.html`等）との実装差分の洗い出しはコード側で別途実施する。

### 現在の実装状況（v0.x）

| 機能 | 状態 |
|------|------|
| 認証（ログイン・新規登録・リセット） | ✅ 完了 |
| オンボーディング | ✅ 完了 |
| ダッシュボード（ミニマル） | ✅ 完了 |
| 蜂群サマリー（Beewiseスタイル縦バー図） | ⚠️ 要作り直し（本設計書のSCR-008/013仕様に刷新） |
| 内検記録（枠式・割合式） | ⚠️ 要作り直し（SCR-012ハイブリッドUI・段構成・記録方式デフォルト化に対応） |
| 内検履歴・詳細表示 | ⚠️ 要作り直し（SCR-016/016b廃止、SCR-009ポップアップ＋SCR-013に統合） |
| 作業記録・履歴 | ✅ 完了（種別別フォーム・写真添付・複数蜂群対応は v1.1 で拡張） |
| カレンダー（作業管理） | ✅ 完了（タブ切替・フィルタ・タスク機能は v1.1 で拡張） |
| 分析（トレンド・比較・レポート） | ✅ 完了（個別群比較・全体平均タブ等は v1.1 で拡張） |
| AI診断入力・結果表示 | ✅ 完了 |
| Push通知（購読・受信） | ✅ 完了（種別ごとの個別設定は v1.1） |
| Realtime同期 | ✅ 完了 |
| PWA（Service Worker・オフライン） | ✅ 完了（操作キューUIは v1.1） |
| テーマシステム（clean / honey 等） | ✅ 完了 |
| データエクスポート | ✅ 完了 |
| 匿名ベンチマーク統計 | ✅ 完了 |

---

### v1.0 — 基盤再構築（今回の詳細設計を反映した作り直し・最優先）

| # | タスク | 優先度 |
|---|--------|--------|
| 0.1 | DBスキーマ更新（`colonies`/`farms`/`insp_records`/`work_records`へのカラム追加、`tasks`テーブル新設） | 高 |
| 0.2 | SCR-013 枠ビューア刷新（段別スクロール表示、積み上げバーの内訳表現、女王ステータス色分け、その場編集、段の追加・削除） | 高 |
| 0.3 | SCR-012 内検記録のハイブリッドUI化（SCR-013ベース＋入力特化コントロール、記録方式の前回値デフォルト化） | 高 |
| 0.4 | SCR-008 蜂群一覧の刷新（スコアではなく実測値の積み上げバー、養蜂場セレクタ・検索・フィルタ・ソート永続化） | 高 |
| 0.5 | SCR-009 統合対応（SCR-016/016b廃止に伴うポップアップ構成変更、内訳推移グラフ） | 高 |
| 0.6 | SCR-025/026 タスク・作業記録の新スキーマ対応（`tasks`テーブル、種別別フォーム、複数蜂群紐づけ） | 中 |

---

### v1.1 — 品質安定化（旧v1.0の残り＋横断的UIパターン）

| # | タスク | 優先度 |
|---|--------|--------|
| 1.1 | 内検記録の編集機能（SCR-012インライン編集方針で実装） | 高 |
| 1.2 | 蜂群・養蜂場の削除確認ダイアログ＋ゴミ箱/アーカイブ機能（`archived_at`活用） | 中 |
| 1.3 | オフライン時の操作キュー（ヘッダーインジケーター＋設定タブでの同期状況確認） | 中 |
| 1.4 | エラー表示の統一（トースト → 画面上部固定のエラーバナー、重大度別表示） | 中 |
| 1.5 | 空状態（空リスト）表示の統一（SCR-006パターンを全画面に適用） | 中 |
| 1.6 | ローディング状態の改善（スケルトン UI） | 低 |
| 1.7 | 作業記録の写真添付（`photo_urls`、SCR-021連携） | 中 |
| 1.8 | 通知設定の種別ごとの個別ON/OFF（SCR-031） | 低 |

---

### v1.5 — UX 強化

| # | タスク | 優先度 |
|---|--------|--------|
| 2.1 | 内検リマインダー（前回から 14 日でプッシュ通知） | 高 |
| 2.2 | 養蜂場マップの実装（SCR-010、蜜源植物ピン含む） | 高 |
| 2.3 | センサーデータの外部連携（SCR-017/018、温度・重量・湿度・音響センサー API） | 中 |
| 2.4 | 蜂群の健康スコア自動算出ロジックの精緻化（季節補正含む） | 中 |
| 2.5 | タスクの繰り返し設定（毎週/毎月/N日ごと） | 中 |
| 2.6 | 養蜂場タブのマップ常設タブ化（SCR-008⇔SCR-010、セグメントコントロール案3への拡張） | 低 |
| 2.7 | 多言語対応の完成（現在の英語切替の全画面対応、今回追加要素も含む） | 低 |

---

### v2.0 — 高度機能

| # | タスク | 優先度 |
|---|--------|--------|
| 3.1 | 実際の AI 連携（Supabase Edge Function + Claude API で診断） | 高 |
| 3.2 | CSV / PDF エクスポートの UI 化（SCR-028/031導線の実装） | 中 |
| 3.3 | 複数養蜂家のチーム共有（共有養蜂場） — 画面仕様は未着手、着手時に別途詰める | 中 |
| 3.4 | Apple Watch / スマートデバイス連携 | 低 |
| 3.5 | ネイティブアプリ化（Capacitor / PWABuilder） | 低 |
| 3.6 | inspection robotとのカメラ連携（通信・同期方式）— 技術詳細は未検討、着手時に別途詰める | 低 |

---

## 付録: ファイル構成

```
HoneyOS/
├── index.html          # アプリ本体（全画面・ロジック・テーマ）
├── supabase_client.js  # DB アクセス層（window.HoneyDB）
├── supabase_schema.sql # テーブル定義・RLS ポリシー
├── support.js          # ユーティリティ
├── utils.js            # 汎用ヘルパー
├── component.js        # 追加コンポーネント
├── sw.js               # Service Worker
├── manifest.json       # PWA マニフェスト
├── icon-192.png        # PWA アイコン
├── icon-512.png        # PWA アイコン
├── hive_good.png       # 蜂群状態アイコン（良好）
├── hive_warn.png       # 蜂群状態アイコン（注意）
├── hive_danger.png     # 蜂群状態アイコン（危険）
├── hive_unknown.png    # 蜂群状態アイコン（不明）
├── DESIGN_SPEC.md      # 本設計書
└── tests/              # テスト
```

---

*このドキュメントはコードベースの実装状況をもとに自動生成されました。実装が進んだ際は適宜更新してください。*
