# Screen references

The design catalog contains device frames and board-level spacing, so it is a visual reference rather than a pixel-comparison baseline.

Before implementing each SCR screen, export or crop a clean screen reference here using the naming pattern `SCR-006-home.png`. Approved implementation screenshots become the Playwright baseline.
